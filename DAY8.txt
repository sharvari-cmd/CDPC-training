%%Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.

from typing import List

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        mp = {}  # value -> index

        for i in range(len(nums)):
            complement = target - nums[i]

            # check if complement exists
            if complement in mp:
                return [mp[complement], i]

            # store current element
            mp[nums[i]] = i

        return []

%% Fizz-Buzz
from typing import List

class Solution:
    def fizzBuzz(self, n: int) -> List[str]:
        answer = []

        for i in range(1, n + 1):
            if i % 3 == 0 and i % 5 == 0:
                answer.append("FizzBuzz")
            elif i % 3 == 0:
                answer.append("Fizz")
            elif i % 5 == 0:
                answer.append("Buzz")
            else:
                answer.append(str(i))

        return answer
%%  Running Sum of 1d Array

lass Solution:
    def runningSum(self, nums):
        res = []
        total = 0

        for i in nums:
            total += i
            res.append(total)

        return res

# implement a stack with push(),pop(),peek(),and display()
# next larger element
# input:
# N=4,arr[]=[1 3 2 4]
# output:
# 3 4 4 -1
# explanation:
# in the array;the next larger elemenyt to 1 is 3,3 is 4,and for 4?
# since it doesnt exist it is -1
def next_greater(arr):
    n = len(arr)
    result = [-1] * n
    stack = []

    for i in range(n):
        while stack and arr[i] > arr[stack[-1]]:
            result[stack.pop()] = arr[i]
        stack.append(i)

    return result


# Input
arr = [1, 3, 2, 4]

# Output
print(*next_greater(arr))
        
